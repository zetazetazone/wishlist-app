---
phase: 32-ui-component-migration
plan: 07
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - components/wishlist/AddItemModal.tsx
  - components/wishlist/AddItemBottomSheet.tsx
  - components/wishlist/LuxuryBottomSheet.tsx
  - components/wishlist/WishlistItemCard.tsx
  - components/wishlist/WishlistItemCardSimple.tsx
  - components/wishlist/LuxuryWishlistCard.tsx
  - components/wishlist/GroupPickerSheet.tsx
  - components/wishlist/ItemTypeBadge.tsx
  - components/wishlist/FavoriteHeart.tsx
  - components/wishlist/MostWantedBadge.tsx
autonomous: true

must_haves:
  truths:
    - "Add item forms display labels in selected language"
    - "Wishlist cards display item type badges in selected language"
    - "Priority and favorite labels display in selected language"
  artifacts:
    - path: "components/wishlist/AddItemBottomSheet.tsx"
      provides: "Translated add item form"
      contains: "useTranslation"
    - path: "components/wishlist/ItemTypeBadge.tsx"
      provides: "Translated item type badge"
      contains: "useTranslation"
    - path: "components/wishlist/LuxuryWishlistCard.tsx"
      provides: "Translated wishlist card"
      contains: "useTranslation"
  key_links:
    - from: "components/wishlist/ItemTypeBadge.tsx"
      to: "src/i18n/locales/en.json"
      via: "t() for type labels"
      pattern: "t\\('wishlist\\.itemType\\."
---

<objective>
Migrate wishlist item components (10 files) to use translations

Purpose: Wishlist components are core UI elements seen throughout the app
Output: All wishlist components display text in selected language
</objective>

<execution_context>
@/home/zetaz/.claude/get-shit-done/workflows/execute-plan.md
@/home/zetaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-ui-component-migration/32-RESEARCH.md
@src/i18n/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate add item modals and sheets</name>
  <files>
    components/wishlist/AddItemModal.tsx
    components/wishlist/AddItemBottomSheet.tsx
    components/wishlist/LuxuryBottomSheet.tsx
    components/wishlist/GroupPickerSheet.tsx
  </files>
  <action>
For each component, apply translation migration:

1. For AddItemModal.tsx:
   - Add `import { useTranslation } from 'react-i18next';`
   - Add `const { t } = useTranslation();`
   - Translate:
     - "Add Item" title: `t('wishlist.addItem')`
     - "Item Title" label: `t('wishlist.itemTitle')`
     - "Item URL" label: `t('wishlist.itemUrl')`
     - "Price" label: `t('wishlist.itemPrice')`
     - "Priority" label: `t('wishlist.itemPriority')`
     - Priority options: `t('wishlist.priority.low')`, `t('wishlist.priority.medium')`, etc.
     - Item type options: `t('wishlist.itemType.gift')`, `t('wishlist.itemType.surpriseMe')`, `t('wishlist.itemType.mysteryBox')`
     - Validation errors: `t('wishlist.errors.titleRequired')`, etc.
     - "Add" / "Save" buttons
   - All Alert.alert() calls

2. For AddItemBottomSheet.tsx:
   - Same pattern as AddItemModal
   - Ensure all form labels, placeholders, and buttons translated
   - All Alert.alert() calls

3. For LuxuryBottomSheet.tsx:
   - Add imports and hook
   - Translate edit item form labels (similar to add item)
   - "Edit Item" title: `t('wishlist.editItem')`
   - "Delete" button: `t('common.delete')`
   - Confirmation alerts for delete
   - All Alert.alert() calls

4. For GroupPickerSheet.tsx:
   - Add imports and hook
   - Translate:
     - "Select Group" or similar header
     - "No groups" empty state: `t('groups.empty.noGroups')`
     - Any action buttons
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `grep -l "useTranslation" components/wishlist/AddItemModal.tsx`
    - `grep -l "useTranslation" components/wishlist/AddItemBottomSheet.tsx`
    - `grep -l "useTranslation" components/wishlist/LuxuryBottomSheet.tsx`
  </verify>
  <done>
    - AddItemModal, AddItemBottomSheet use translations
    - LuxuryBottomSheet uses translations
    - GroupPickerSheet uses translations
    - All form labels, errors, and buttons translated
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate wishlist cards and badges</name>
  <files>
    components/wishlist/WishlistItemCard.tsx
    components/wishlist/WishlistItemCardSimple.tsx
    components/wishlist/LuxuryWishlistCard.tsx
    components/wishlist/ItemTypeBadge.tsx
    components/wishlist/FavoriteHeart.tsx
    components/wishlist/MostWantedBadge.tsx
  </files>
  <action>
1. For WishlistItemCard.tsx:
   - Add imports and hook
   - Translate any visible labels (price formatting may use Intl.NumberFormat)
   - Translate action buttons if present
   - All Alert.alert() calls for delete confirmation

2. For WishlistItemCardSimple.tsx:
   - Add imports and hook
   - Similar to WishlistItemCard
   - All Alert.alert() calls

3. For LuxuryWishlistCard.tsx:
   - Add imports and hook
   - Translate:
     - Priority labels if shown as text
     - Any action buttons
     - Delete confirmation
   - All Alert.alert() calls (this file likely has several)

4. For ItemTypeBadge.tsx:
   - Add imports and hook
   - Translate type labels:
     - "Gift": `t('wishlist.itemType.gift')`
     - "Surprise Me": `t('wishlist.itemType.surpriseMe')`
     - "Mystery Box": `t('wishlist.itemType.mysteryBox')`

5. For FavoriteHeart.tsx:
   - Add imports and hook
   - Translate accessibility labels if present
   - "Mark as Favorite": `t('wishlist.favorite.markFavorite')`
   - "Remove Favorite": `t('wishlist.favorite.removeFavorite')`

6. For MostWantedBadge.tsx:
   - Add imports and hook
   - Translate "Most Wanted" text: `t('wishlist.favorite.mostWanted')`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `grep -l "useTranslation" components/wishlist/LuxuryWishlistCard.tsx`
    - `grep -l "useTranslation" components/wishlist/ItemTypeBadge.tsx`
    - `grep -l "useTranslation" components/wishlist/MostWantedBadge.tsx`
  </verify>
  <done>
    - All 6 wishlist card/badge components use translations
    - Item type badges display translated labels
    - Most Wanted badge displays translated text
    - All Alert.alert() calls translated
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. All 10 wishlist components use useTranslation
3. Run app, switch to Spanish, verify wishlist components show Spanish
</verification>

<success_criteria>
- All 10 wishlist components migrated to use translations
- Add item forms display in selected language
- Item type badges display "Regalo" / "Sorpréndeme" / "Caja Misteriosa" in Spanish
- Most Wanted badge displays "Más Deseado" in Spanish
- No hardcoded English strings remain in wishlist components
</success_criteria>

<output>
After completion, create `.planning/phases/32-ui-component-migration/32-07-SUMMARY.md`
</output>
